/*
Exercise 10.12: Write a function named compareIsbn 
that compares the isbn() members of two Sales_data objects. 
Use that function to sort a vector that holds Sales_data objects.
*/
#include <iostream> 
#include <algorithm>
#include <vector>
#include "Sales_data.h"
bool compareIsbn(cosnt Sales_data & data1,const Sales_data & data2);


int main()
{
	Sales_data data1,data2;
	std::vector ;
	return 0;
}
bool compareIsbn(cosnt Sales_data & data1,const Sales_data & data2)
{
	return 
}
